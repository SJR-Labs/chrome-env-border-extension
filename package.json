{
  "name": "env-border-extension",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "zip": "npm run build && cd dist && zip -r ../env-border-extension.zip . && cd .."
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.2.0",
    "vite-plugin-static-copy": "^3.0.0"
  }
}
